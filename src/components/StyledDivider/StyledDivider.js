import React from 'react';
import { Divider } from '@material-ui/core';

const StyledDivider = () => {
    return (
        <Divider style={{ backgroundColor: '#ffffff', opacity: '0.39', height: '1px' }} />
    );
};

export default StyledDivider;